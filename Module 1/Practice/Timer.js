// let timerId = setTimeout(() => alert("never happens"), 1000);
// alert(timerId); // timer identifier

// clearTimeout(timerId);
// alert(timerId); // same identifier (doesn't become null after canceling)

// let timerId = setInterval(() => alert('tick'), 2000);

// // after 5 seconds stop
// setTimeout(() => { clearInterval(timerId); alert('stop'); }, 5000);



//1. Create a node.js application that contains Home Page, Contact Us and Support. When clicking on support, a chatbot should open up which will say Hii.

//2. create a duplex stream

//3. Write a program using socket.io to send "wow server" message from client to the server after every 2 seconds but after 12 seconds the client must stop sending this "wow server" message to the server. After waiting for another 5 seconds, the client again must start sending the message to the server.

//4. Create a node application to parse a JSON file and print the contents of file on console